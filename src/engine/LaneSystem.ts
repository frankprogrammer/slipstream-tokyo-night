import { CONFIG } from '../config';

/**
 * LaneSystem — 3-lane grid + swipe/keyboard input.
 * Lane X positions: -LANE_WIDTH, 0, +LANE_WIDTH.
 * Swipe: pointerdown/up with threshold + max time.
 * Keyboard: arrows/AD for dev testing.
 */
export class LaneSystem {
  // TODO: Implement
}
