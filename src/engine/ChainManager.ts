import { CONFIG } from '../config';

/**
 * ChainManager — Chain multiplier + milestones.
 * Increments on draft-complete. Resets on collision or CHAIN_TIMEOUT without drafting.
 * Milestones at ×5, ×10, ×15, ×20 trigger celebrations.
 */
export class ChainManager {
  // TODO: Implement
}
