/**
 * ShareCard — Generates shareable end-of-run image.
 *
 * Uses renderer.domElement.toDataURL() to capture the frozen 3D scene,
 * then composites score/chain text on top via a 2D canvas overlay.
 *
 * Triggered by share button on game over screen.
 */
export class ShareCard {
  // TODO: Implement
}
