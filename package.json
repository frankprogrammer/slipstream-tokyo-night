{
  "name": "slipstream-tokyo-night",
  "version": "0.1.0",
  "description": "A 3D endless taxi racer through neon-lit Tokyo streets — chain slipstreams to build speed",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
